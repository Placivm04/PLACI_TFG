\chapter{Análisis de Modelos y Búsqueda de Hiperparámetros}

En este sección se presentan los resultados obtenidos tras la aplicación de Optuna para la optimización de hiperparámetros en los modelos entrenados con los datasets mencionados en la sección anterior. Se ha seguido un enfoque de optimización multiobjetivo, buscando un equilibrio entre la precisión del modelo (medida a través del F1-Score) y su eficiencia (medida a través de la latencia de inferencia).

El procedimiento seguido es el siguiente:

\begin{enumerate}
    \item Para cada modelo, se ha definido un espacio de búsqueda de hiperparámetros específico, adaptado a las características de cada algoritmo.
    \item Se ha utilizado validación cruzada estratificada para evaluar el rendimiento de cada configuración de hiperparámetros, asegurando una evaluación robusta y representativa del modelo.
    \item Se han optimizado simultáneamente dos objetivos: el F1-Score (para maximizar la precisión) y la latencia de inferencia (para minimizar el tiempo de respuesta).
    \item Se ha identificado la frontera de Pareto para cada modelo, lo que permite visualizar el compromiso entre precisión y eficiencia y seleccionar configuraciones que se ajusten a diferentes necesidades.
    \item Finalmente, se han evaluado los candidatos seleccionados de la frontera de Pareto en el conjunto de test para obtener una comparativa final de su rendimiento y latencia.
\end{enumerate}

Para la evaluación final de los modelos seleccionados se utilizó un procedimiento de benchmarking instrumentado. En primer lugar, el conjunto de test se dividió en bloques o paquetes de muestras, de forma que la inferencia no se realizó mediante una única llamada global al modelo, sino procesando secuencialmente dichos bloques. Esta decisión permitió monitorizar el proceso durante la predicción y obtener métricas adicionales de consumo, como el incremento de memoria RAM.

% La latencia se calculó a partir del tiempo total necesario para procesar todos los bloques del conjunto de test, dividiendo dicho tiempo entre el número total de muestras evaluadas. Por tanto, la latencia reportada representa el tiempo medio de clasificación por muestra dentro de una ejecución monitorizada. De forma complementaria, el throughput se obtuvo dividiendo el número total de muestras entre el tiempo total de inferencia.

% Este procedimiento permite evaluar no solo la velocidad de predicción del modelo, sino también su comportamiento en términos de uso de recursos durante la ejecución. No obstante, al incluir monitorización de memoria y procesamiento por bloques, las métricas temporales obtenidas reflejan el rendimiento del benchmark instrumentado y pueden diferir de una medición de inferencia pura realizada mediante una única llamada a \texttt{predict}, como veremos posteriormente.

Antes de presentar los resultados finales, es importante señalar que el proceso completo de selección de hiperparámetros se recoge en el Apéndice~\ref{apendice}. En dicho apéndice se incluyen las fronteras de Pareto y las tablas completas de candidatos evaluados para cada combinación de modelo y dataset.

De forma general, las fronteras de Pareto obtenidas muestran un comportamiento de saturación en gran parte de los modelos evaluados (forma exponencial), lo que indica que a partir de cierto punto, las mejoras en F1-Score se logran a costa de incrementos significativos en la latencia.
No obstante, este patrón no aparece con la misma claridad en todos los modelos. En algunos casos, como SVM ~\ref{fig:pareto_svm} en UNSW-NB15, la frontera de Pareto no muestra una curva de saturación tan definida. En este caso, los puntos aparecen más agrupados y las diferencias de latencia entre configuraciones son muy pequeñas, por lo que no se aprecia una relación tan evidente entre el aumento del rendimiento y el coste temporal. Este comportamiento puede deberse a varios factores, como la propia naturaleza del modelo, la distribución de los datos del dataset, el rango de hiperparámetros explorado por Optuna o la escasa variación de latencia entre candidatos.

La elección del \textbf{candidato ganador} se ha realizado mediante una observación conjunta de la frontera de Pareto. No se ha seleccionado siempre el candidato con mayor F1-score, sino aquel que ofrecía el mejor equilibrio entre F1-score y latencia.

\section{Optimización de hiperparámetros estructurales}

El ajuste de hiperparámetros se centra específicamente en aquellos que determinan la \textbf{estructura del modelo}. Los hiperparámetros estructurales influyen directamente en la complejidad computacional, el número de operaciones necesarias para generar una predicción y el tamaño final del modelo.

A diferencia de otros hiperparámetros relacionados con el proceso de entrenamiento (como la tasa de aprendizaje o el optimizador), los \textbf{hiperparámetros estructurales determinan la arquitectura final del modelo} y, por tanto, pueden influir de forma directa o relevante en su coste en tiempo de ejecución una vez desplegado.

A continuación, se describen los hiperparámetros estructurales más relevantes para cada uno de los modelos considerados en este trabajo.

\subsubsection{Random Forest}

En el caso de Random Forest, el coste de inferencia depende principalmente de:

\begin{itemize}
    \item \textbf{n\_estimators}: número de árboles que componen el bosque. Un mayor número de árboles incrementa linealmente el número de recorridos necesarios para cada predicción.
    \item \textbf{max\_depth}: profundidad máxima de cada árbol. Árboles más profundos implican más comparaciones por predicción.
\end{itemize}

\subsubsection{XGBoost}

En XGBoost, la inferencia consiste en recorrer secuencialmente todos los árboles entrenados y sumar sus contribuciones. Los hiperparámetros estructurales más relevantes son:

\begin{itemize}
    \item \textbf{n\_estimators}: número total de árboles del modelo.
    \item \textbf{max\_depth}: profundidad máxima de cada árbol.
\end{itemize}

\subsubsection{LightGBM}

En LightGBM, el coste de inferencia también está influenciado por:
\begin{itemize}
    \item \textbf{n\_estimators}: número de árboles en el modelo.
    \item \textbf{max\_depth}: profundidad máxima de cada árbol.
    \item \textbf{num\_leaves}: número máximo de hojas por árbol, que afecta la complejidad del modelo y el número de comparaciones necesarias para cada predicción.
\end{itemize}

\subsubsection{CatBoost}

En CatBoost, los hiperparámetros estructurales que afectan el coste de inferencia son:

\begin{itemize}
    \item \textbf{iterations}: número de árboles a entrenar. Un mayor número de iteraciones incrementa linealmente el coste computacional en inferencia, ya que cada predicción requiere recorrer todos los árboles.
    \item \textbf{depth}: profundidad máxima de cada árbol. Árboles más profundos implican más comparaciones y operaciones por predicción.
\end{itemize}


\subsubsection{Support Vector Machine (SVM)}

Para SVM, el coste de inferencia depende principalmente del tipo de kernel utilizado. En este trabajo se prioriza el uso de:

\begin{itemize}
    \item \textbf{C}: parámetro de regularización que puede influir en la complejidad del modelo, aunque su impacto en la inferencia es menor que el tipo de kernel.
\end{itemize}

\subsubsection{Multilayer Perceptron (MLP)}

En redes MLP, el coste de inferencia está directamente relacionado con el número total de pesos del modelo. Los hiperparámetros estructurales clave son:

\begin{itemize}
    \item \textbf{hidden\_layer\_sizes}: número de capas ocultas y número de neuronas por capa.
\end{itemize}

% \subsubsection{Long Short-Term Memory (LSTM)}

% En las redes LSTM, la complejidad de inferencia depende de:

% \begin{itemize}
%     \item \textbf{lstm\_units}: número de unidades LSTM en la capa recurrente.
%     \item \textbf{time\_steps}: número de pasos temporales procesados.
%     \item \textbf{num\_layers}: número de capas en la red.
% \end{itemize}


\subsubsection{Convolutional Neural Networks (CNN 1D)}

En las CNN 1D utilizadas en este trabajo, los hiperparámetros estructurales más relevantes son:

\begin{itemize}
    \item \textbf{n\_filters}: número de filtros en las capas convolucionales.
    \item \textbf{kernel\_size}: tamaño del kernel.
    \item \textbf{dense\_units}: número de unidades en la capa densa.
\end{itemize}



\section{Modelos entrenados con UNSW-NB15}

% AQUÍ IRÍA TODO EL DESARROLLO DE LOS MODELOS
% \subsection{Comparativa Modelos con Dataset UNSW-NB15}

En esta sección se presetan los resultados obtenidos tras la optimización de hiperparámetros estructurales para cada modelo entrenado con el dataset UNSW-NB15. Se muestra la configuración ganadora de cada modelo, seguida de una comparativa global de benchmarking, además de la curva ROC y las matrices de confusión correspondientes a cada modelo.
La combinación de hiperparámetros ganadores para cada modelo se muestra en la siguiente tabla:

\begin{table}[H]
    \centering
    \small
    \begin{tabular}{|l|p{10cm}|}
        \hline
        \textbf{Modelo} & \textbf{Hiperparámetros ganadores (UNSW-NB15)} \\
        \hline
        Random Forest & n\_estimators = 50, max\_depth = 23 \\
        \hline
        XGBoost & n\_estimators = 200, max\_depth = 11 \\
        \hline
        LightGBM & n\_estimators = 100, num\_leaves = 145, max\_depth = 12 \\
        \hline
        CatBoost & n\_estimators = 500, max\_depth = 10 \\
        \hline
        SVM & C = 0.000187 \\
        \hline
        MLP & n\_layers = 3, unidades = [96, 32, 64] \\
        \hline
        CNN-1D & n\_filters = 64, kernel\_size = 5, dense\_units = 48 \\
        \hline
    \end{tabular}
    \caption{Hiperparámetros ganadores de los modelos evaluados con UNSW-NB15}
    \label{tab:unsw_best_hyperparams}
\end{table}
 
Tras hacer la fase de benchmarking con los mejores candidatos de los diferentes modelos, hemos obtenido los siguientes resultados:

\begin{table}[H]
    \centering
    \small
    \begin{tabular}{|l|c|c|c|c|c|}
        \hline
        \textbf{Modelo} & \textbf{Latencia (ms)} & \textbf{Throughput (paq/s)} & \textbf{Incremento RAM (MB)} & \textbf{CPU Efectiva (\%)} & \textbf{F1-Score} \\
        \hline
        Linear SVM & 0.00014 & $7.315991 \times 10^{6}$ & 0.09 & 0.9 & 0.7092 \\
        \hline
        LightGBM & 0.00064 & $1.551954 \times 10^{6}$ & 0.13 & 100 & 0.8592 \\
        \hline
        XGBoost & 0.00067 & $1.492419 \times 10^{6}$ & 0.0 & 100 & 0.8579 \\
        \hline
        CatBoost & 0.00156 & 641453.0 & 1.0 & 50.1 & 0.8570 \\
        \hline
        Random Forest & 0.00889 & 112423.0 & 9.04 & 1.6 & 0.8606 \\
        \hline
        TensorFlow MLP & 0.02443 & 40930.0 & 13.14 & 1.5 & 0.7862 \\
        \hline
        CNN-1D & 0.04931 & 20281.0 & 45.61 & 1.7 & 0.7665 \\
        \hline
    \end{tabular}
    \caption{Comparativa global de rendimiento de modelos - UNSW-NB15}
    \label{tab:hardware_benchmark_global}
\end{table}

A partir de la comparativa global de rendimiento, se observa que los modelos basados en árboles presentan el mejor equilibrio entre capacidad predictiva y eficiencia computacional. Random Forest, LightGBM, XGBoost y CatBoost alcanzan los valores más altos de F1-score. Entre ellos, LightGBM y XGBoost destacan por ofrecer una latencia muy reducida y un throughput elevado, mientras que Random Forest obtiene un F1-score ligeramente superior, aunque con una latencia mayor.

Por otro lado, Linear SVM presenta la menor latencia de inferencia, pero también el F1-score más bajo entre los modelos evaluados. Esto indica que, aunque resulta muy eficiente desde el punto de vista temporal, su formulación lineal limita su capacidad para capturar relaciones no lineales presentes en el dataset. Los modelos neuronales, MLP y CNN-1D, obtienen resultados intermedios en términos de F1-score, pero con un coste de inferencia superior al de los modelos basados en árboles, especialmente en el caso de CNN-1D.

En cuanto al consumo de recursos, modelos como CNN-1D y MLP presentan un mayor incremento de memoria RAM, especialmente CNN-1D, que requiere más memoria durante la inferencia que los modelos clásicos. Por el contrario, SVM, XGBoost, LightGBM y CatBoost presentan una huella de memoria más reducida. Respecto al uso de CPU, LightGBM y XGBoost acaparan el 100\% de la CPU durante la inferencia, puede explicarse por la propia implementación de estas librerías, pues son modelos diseñados para ser eficientes y paralelizables ~\cite{xgboost_pararell} ~\cite{lightgbm_pararell}.

La curva ROC de cada modelo se muestra a continuación:

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{images/ROC_MODELOS.png}
    \caption{Curva ROC - Modelos}
    \label{fig:roc_modelos}
\end{figure}

La curva ROC confirma la tendencia observada en la tabla anterior, mostrando que los modelos basados en árboles alcanzan las áreas bajo la curva más elevadas, lo que indica una mejor capacidad de discriminación entre clases. SVM presenta un rendimiento inferior en términos de AUC, mientras que los modelos neuronales muestran un rendimiento intermedio.

Las matrices de confusión nos permiten observar on más detalle los errores cometidos por cada modelo. Se presentan a continuación:

\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/MC/MC_RF.png}
        \caption{MC - Random Forest}
        \label{fig:mc_rf}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/MC/MC_XGBOOST.png}
        \caption{MC - XGBoost}
        \label{fig:mc_xgboost}
    \end{minipage}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/MC/MC_LIGHTGBM.png}
        \caption{MC - LightGBM}
        \label{fig:mc_lightgbm}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/MC/MC_CATBOOST.png}
        \caption{MC - CatBoost}
        \label{fig:mc_catboost}
    \end{minipage}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/MC/MC_SVM.png}
        \caption{MC - SVM}
        \label{fig:mc_svm}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/MC/MC_MLP.png}
        \caption{MC - MLP}
        \label{fig:mc_mlp}
    \end{minipage}
\end{figure}

\begin{figure}[H]
    \centering
    % \begin{minipage}{0.48\textwidth}
    %     \centering
    %     \includegraphics[width=\textwidth]{images/MC/MC_LSTM.png}
    %     \caption{MC - LSTM}
    %     \label{fig:mc_lstm}
    % \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/MC/MC_CNN.png}
        \caption{MC - CNN}
        \label{fig:mc_cnn}
    \end{minipage}
\end{figure}

\section{Modelos entrenados con IOT-23}

Para completar el análisis y las comparaciones, es interesante ver cómo se comportan los modelos con un dataset adaptado a un entorno de Internet de las Cosas (IoT), que suele presentar características muy diferentes a los datasets tradicionales de tráfico de red.
En este caso, se han entrenado los mismos modelos que el apartado anterior, pero usando este dataset.

% AQUÍ IRÍA TODO EL DESARROLLO DE LOS MODELOS
% \subsection{Comparativa Modelos con Dataset IOT-23}

Tras hacer una fase de benchmarking con los mejores candidatos de los diferentes modelos, hemos obtenido los siguientes resultados:

La tabla de los mejores hiperparámetros obtenidos para cada modelo con el dataset IOT-23 es la siguiente:

\begin{table}[H]
    \centering
    \small
    \begin{tabular}{|l|p{10cm}|}
        \hline
        \textbf{Modelo} & \textbf{Hiperparámetros ganadores (IOT-23)} \\
        \hline
        Random Forest & n\_estimators = 50, max\_depth = 21 \\
        \hline
        XGBoost & n\_estimators = 50, max\_depth = 8 \\
        \hline
        LightGBM & n\_estimators = 50, num\_leaves = 15, max\_depth = 6 \\
        \hline
        CatBoost & n\_estimators = 100, max\_depth = 8 \\
        \hline
        SVM & C = 0.801714 \\
        \hline
        MLP & n\_layers = 3, unidades = [32, 48, 48] \\
        \hline
        CNN-1D & n\_filters = 64, kernel\_size = 4, dense\_units = 80 \\
        \hline
    \end{tabular}
    \caption{Hiperparámetros ganadores de los modelos evaluados con IOT-23}
    \label{tab:iot_best_hyperparams}
\end{table}

\begin{table}[H]
    \centering
    \small
    \begin{tabular}{|l|c|c|c|c|c|}
        \hline
        \textbf{Modelo} & \textbf{Latencia (ms)} & \textbf{Throughput (paq/s)} & \textbf{Incremento RAM (MB)} & \textbf{CPU Efectiva (\%)} & \textbf{F1-Score} \\
        \hline
        Linear SVM & 0.00018 & $5.684672 \times 10^{6}$ & 2.01 & 1.1 & 0.955 \\
        \hline
        LightGBM & 0.00047 & $2.144772 \times 10^{6}$ & 0.11 & 100 & 0.9998 \\
        \hline
        XGBoost & 0.00584 & 171225 & 0.31 & 96.2 & 0.9991 \\
        \hline
        CatBoost & 0.00102 & 982856 & 0.02 & 69.2 & 0.9999 \\
        \hline
        Random Forest & 0.00892 & 112103 & 13.39 & 1.4 & 0.9997 \\
        \hline
        TensorFlow MLP & 0.02377 & 42079 & 0.72 & 1.5 & 0.9956 \\
        \hline
        CNN-1D & 0.04959 & 20153 & 8.25 & 1.8 & 0.9952 \\
        \hline
    \end{tabular}
    \caption{Comparativa global de rendimiento de modelos - IOT-23}
    \label{tab:hardware_benchmark_global_iot23}
\end{table}

En el caso de IoT-23, la tabla de rendimiento muestra valores de F1-score muy elevados en la mayoría de modelos, especialmente en los clasificadores basados en árboles. Random Forest, XGBoost, LightGBM y CatBoost alcanzan resultados cercanos a 1, lo que indica una gran capacidad para distinguir entre tráfico benigno y malicioso en este dataset. Además, estos modelos mantienen latencias reducidas y throughputs elevados, por lo que ofrecen un compromiso muy favorable entre rendimiento predictivo y eficiencia.

Los modelos neuronales, MLP y CNN-1D, también presentan un rendimiento muy competitivo, aunque con un coste de inferencia superior, especialmente en el caso de CNN-1D. Por su parte, SVM vuelve a destacar por su rapidez, pero su F1-score queda por debajo del resto de modelos.

Respecto al consumo de recursos, se observa que Random Forest es el modelo con mayor incremento de RAM, seguido de CNN-1D. En términos de uso de CPU, al igual que con UNSW-NB15, LightGBM y XGBoost son los modelos que mayor porcentaje de uso de CPU acaparan.

La curva ROC de los modelos evaluados con el dataset IOT-23 es la siguiente:
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{images/IOT/ROC_MODELOS.png}
    \caption{Curva ROC - Modelos (IOT-23)}
    \label{fig:roc_modelos_iot}
\end{figure}

Las curvas ROC obtenidas confirman una excelente separación entre clases en la mayoría de modelos, coherentes con los valores de F1-score observados en la tabla anterior.

Las matrices de confusión de cada modelo son las siguientes:

\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/IOT/MC/MC_RF_IOT.png}
        \caption{MC - Random Forest (IOT-23)}
        \label{fig:mc_rf_iot}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/IOT/MC/MC_XGBOOST_IOT.png}
        \caption{MC - XGBoost (IOT-23)}
        \label{fig:mc_xgboost_iot}
    \end{minipage}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/IOT/MC/MC_LIGHTGBM_IOT.png}
        \caption{MC - LightGBM (IOT-23)}
        \label{fig:mc_lightgbm_iot}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/IOT/MC/MC_CATBOOST_IOT.png}
        \caption{MC - CatBoost (IOT-23)}
        \label{fig:mc_catboost_iot}
    \end{minipage}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/IOT/MC/MC_SVM_IOT.png}
        \caption{MC - SVM (IOT-23)}
        \label{fig:mc_svm_iot}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/IOT/MC/MC_MLP_IOT.png}
        \caption{MC - MLP (IOT-23)}
        \label{fig:mc_mlp_iot}
    \end{minipage}
\end{figure}

\begin{figure}[H]
    \centering
    % \begin{minipage}{0.48\textwidth}
    %     \centering
    %     \includegraphics[width=\textwidth]{images/IOT/MC/MC_LSTM_IOT.png}
    %     \caption{MC - LSTM (IOT-23)}
    %     \label{fig:mc_lstm_iot}
    % \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/IOT/MC/MC_CNN_IOT.png}
        \caption{MC - CNN (IOT-23)}
        \label{fig:mc_cnn_iot}
    \end{minipage}
\end{figure}


\section{Modelos entrenados con ToN-IoT}

Una de las sospechas de los resultados obtenidos con el dataset IOT-23 es que en este dataset la mayoría de muestras de ataque son de escaneos horizontales de puertos, lo que hace que el modelo pueda aprender a detectar ataques simplemente con la información de los puertos, sin necesidad de aprender patrones más complejos. Para comprobar esta hipótesis, se ha entrenado el mismo proceso de entrenamiento y benchmarking con el dataset ToN-IoT, que es un dataset de IoT con una mayor variedad de ataques y características.

% \subsection{Comparativa Modelos con Dataset ToN-IoT}


La tabla de los mejores hiperparámetros obtenidos para cada modelo con el dataset ToN-IoT es la siguiente:

\begin{table}[H]
    \centering
    \small
    \begin{tabular}{|l|p{10cm}|}
        \hline
        \textbf{Modelo} & \textbf{Hiperparámetros ganadores (ToN-IoT)} \\
        \hline
        Random Forest & n\_estimators = 50, max\_depth = 18 \\
        \hline
        XGBoost & n\_estimators = 50, max\_depth = 7 \\
        \hline
        LightGBM & n\_estimators = 100, num\_leaves = 233, max\_depth = 11 \\
        \hline
        CatBoost & n\_estimators = 250, max\_depth = 9 \\
        \hline
        SVM & C = 2.870875 \\
        \hline
        MLP & n\_layers = 3, unidades = [64, 16, 64] \\
        \hline
        CNN-1D & n\_filters = 96, kernel\_size = 5, dense\_units = 96 \\
        \hline
    \end{tabular}
    \caption{Hiperparámetros ganadores de los modelos evaluados con ToN-IoT}
    \label{tab:ton_best_hyperparams}
\end{table}

Tras hacer una fase de benchmarking con los mejores candidatos de los diferentes modelos, hemos obtenido los siguientes resultados:

\begin{table}[H]
    \centering
    \small
    \begin{tabular}{|l|c|c|c|c|c|}
        \hline
        \textbf{Modelo} & \textbf{Latencia (ms)} & \textbf{Throughput (paq/s)} & \textbf{Incremento RAM (MB)} & \textbf{CPU Efectiva (\%)} & \textbf{F1-Score} \\
        \hline
        Linear SVM & 0.00019 & $5.185815 \times 10^{6}$ & 0.02 & 1.3 & 0.9522 \\
        \hline
        LightGBM & 0.0003 & $3.333129 \times 10^{6}$ & 0.0 & 100 & 0.9977 \\
        \hline
        XGBoost & 0.00065 & $1.536528 \times 10^{6}$ & 0.2 & 100 & 0.9986 \\
        \hline
        CatBoost & 0.00129 & 773537.0 & 0.03 & 57.3 & 0.9985 \\
        \hline
        Random Forest & 0.00886 & 112889.0 & 8.04 & 1.5 & 0.9983 \\
        \hline
        TensorFlow MLP & 0.02633 & 37985.0 & 1.92 & 1.5 & 0.9829 \\
        \hline
        CNN-1D & 0.0531 & 18831.0 & 89.74 & 2.4 & 0.9826 \\
        \hline
    \end{tabular}
    \caption{Comparativa global de rendimiento de modelos - IOT-23}
    \label{tab:hardware_benchmark_global_ton}
\end{table}

En TON-IoT, los modelos basados en árboles vuelven a situarse entre las mejores alternativas, combinando valores elevados de F1-score con tiempos de inferencia reducidos. XGBoost, CatBoost, Random Forest y LightGBM muestran un rendimiento muy alto, lo que confirma la eficacia de este tipo de modelos en datasets tabulares de detección de intrusiones.

SVM, de nuevo, mantiene una latencia muy baja, pero obtiene un F1-score inferior al de los modelos basados en árboles. Los modelos neuronales, MLP y CNN-1D, presentan un comportamiento competitivo, aunque con mayor coste temporal. 

Si miramos el consumo de recursos, CNN-1D destaca como el modelo con mayor incremento de RAM. En cuanto al uso de CPU, LightGBM y XGBoost vuelven a ser los modelos que muestras mayores porcentajes durante la fase de inferencia.

La curva ROC de los modelos evaluados con el dataset ToN-IoT es la siguiente:

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{images/TON/ROC_MODELOS_TON.png}
    \caption{Curva ROC - Modelos (ToN-IoT)}
    \label{fig:roc_modelos_ton}
\end{figure}

Al igual que el dataset anterior, las curvas ROC obtenidas muestran una excelente capacidad de discriminación entre clases para la mayoría de modelos, especialmente los basados en árboles, que alcanzan áreas bajo la curva cercanas a 1, lo que es coherente con los valores de F1-score observados en la tabla de rendimiento.

Las matrices de confusión de cada modelo se muestran a continuación:

\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/TON/MC/MC_RF_TON.png}
        \caption{MC - Random Forest (ToN-IoT)}
        \label{fig:mc_rf_ton}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/TON/MC/MC_XGBOOST_TON.png}
        \caption{MC - XGBoost (ToN-IoT)}
        \label{fig:mc_xgboost_ton}
    \end{minipage}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/TON/MC/MC_LIGHTGBM_TON.png}
        \caption{MC - LightGBM (ToN-IoT)}
        \label{fig:mc_lightgbm_ton}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/TON/MC/MC_CATBOOST_TON.png}
        \caption{MC - CatBoost (ToN-IoT)}
        \label{fig:mc_catboost_ton}
    \end{minipage}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/TON/MC/MC_SVM_TON.png}
        \caption{MC - SVM (ToN-IoT)}
        \label{fig:mc_svm_ton}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/TON/MC/MC_MLP_TON.png}
        \caption{MC - MLP (ToN-IoT)}
        \label{fig:mc_mlp_ton}
    \end{minipage}
\end{figure}

\begin{figure}[H]
    \centering
    % \begin{minipage}{0.48\textwidth}
    %     \centering
    %     \includegraphics[width=\textwidth]{images/TON/MC/MC_LSTM_TON.png}
    %     \caption{MC - LSTM (ToN-IoT)}
    %     \label{fig:mc_lstm_ton}
    % \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/TON/MC/MC_CNN_TON.png}
        \caption{MC - CNN (ToN-IoT)}
        \label{fig:mc_cnn_ton}
    \end{minipage}
\end{figure}

\section{Potencial de Análisis en Tiempo Real}

Como se ha indicado anteriormente, los benchmarks realizados en el servidor tenían como objetivo medir no solo el tiempo de inferencia, sino también el uso de CPU y memoria RAM durante la predicción. Por este motivo, las métricas temporales obtenidas en dichos benchmarks incluyen la sobrecarga asociada al procesamiento por bloques y a la monitorización del proceso.

Para complementar ese análisis, en esta sección se realiza una medición adicional centrada exclusivamente en el tiempo puro de inferencia. El objetivo es estimar la capacidad máxima de procesamiento de los modelos una vez cargados en memoria, por lo que la comparación se centra principalmente en el \textit{throughput}, expresado como el número de muestras procesadas por segundo.

Además, esta medición se ha repetido en un equipo local, sin utilizar el servidor de altas prestaciones empleado durante las fases anteriores. De esta forma, se pretende analizar y comparar el comportamiento de los modelos en un entorno más limitado y cercano a un posible escenario real de despliegue, donde los recursos disponibles pueden ser inferiores a los del entorno de entrenamiento.

\subsection{Resultados UNSW-NB15}

\begin{table}[H]
\centering
\caption{Comparativa de F1-score y throughput de inferencia pura para UNSW-NB15}
\label{tab:inferencia_unsw}
\begin{tabular}{lrrr}
\toprule
\textbf{Modelo} & \textbf{F1-Score} & \textbf{Throughput Portátil} & \textbf{Throughput Servidor} \\
\midrule
RF       & 0.8606 & 1209890  & 3721179 \\
XGBoost  & 0.8579 & 1113803  & 9916876 \\ 
CatBoost & 0.8570 & 1639079  & 3058951 \\
LightGBM & 0.8592 & 1260820  & 5452214 \\
SVM      & 0.7092 & 16600905 & 31271516 \\
MLP      & 0.7862 & 1150412  & 1199516 \\
CNN      & 0.7665 & 225353   & 646466 \\ 
\bottomrule
\end{tabular}%
\end{table}

\subsection{Resultados IOT-23}

\begin{table}[H]
\centering
\caption{Comparativa de F1-score y throughput de inferencia pura para IoT-23}
\label{tab:inferencia_iot23}
\begin{tabular}{lrrr}
\toprule
\textbf{Modelo} & \textbf{F1-Score} & \textbf{Throughput Portátil} & \textbf{Throughput Servidor} \\
\midrule
RF       & 0.9997 & 1149778 & 5684020 \\
XGBoost  & 0.9991 & 5391713 & 48100521 \\
CatBoost & 0.9999 & 3859140 & 4200133 \\
LightGBM & 0.9998 & 3932579 & 8290807 \\
SVM      & 0.9550 & 5323551 & 9656967 \\
MLP      & 0.9956 & 1961042 & 2278408 \\
CNN      & 0.9952 & 187062  & 629180 \\
\bottomrule
\end{tabular}%
\end{table}

\subsection{Resultados TON-IOT}

\begin{table}[H] 
\centering
\caption{Comparativa de F1-score y throughput de inferencia pura para TON-IoT}
\label{tab:inferencia_toniot}
\begin{tabular}{lrrr}
\toprule
\textbf{Modelo} & \textbf{F1-Score} & \textbf{Throughput Portátil} & \textbf{Throughput Servidor} \\
\midrule
RF       & 0.9983 & 1401017 & 3924738 \\
XGBoost  & 0.9986 & 2605794 & 41432520 \\
CatBoost & 0.9985 & 2313078 & 3063462\\
LightGBM & 0.9977 & 1259440 & 5034803 \\
SVM      & 0.9522 & 5113359 & 10344547 \\
MLP      & 0.9829 & 837747  & 1133827 \\
CNN      & 0.9826 & 98625  & 334864 \\
\bottomrule
\end{tabular}%
\end{table}

\subsection{Comparación con cargas de red de referencia}

Una vez que hemos obtenido los resultados de throughput para cada modelo en los tres datasets, es interesante compararlos con las cargas de red típicas que se pueden encontrar en diferentes escenarios, para así estimar qué modelo nos convendría más en función del entorno de despliegue y del volumen de tráfico que se espera analizar.

Para ello, se han tomado como referencia varios escenarios: una red IoT local básica de 21 dispositivos ~\cite{cargas_iot}, una red universitaria ~\cite{cargas_universitarias} y distintos casos de pequeñas y medianas empresas ~\cite{cargas_pymes}.

\begin{table}[H]
\centering
\caption{Cargas de red de referencia expresadas en paquetes por segundo}
\label{tab:cargas_red_referencia}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lrrr}
\toprule
\textbf{Escenario} & \textbf{Tráfico considerado} & \textbf{Tamaño medio paquete} & \textbf{Carga estimada (paq/s)} \\
\midrule
Red IoT local básica -- caso mínimo & 400 Kbps & 225 bytes & 222 \\
Red IoT local básica -- caso intermedio & 400 Kbps & 75 bytes & 667 \\
Red IoT local básica -- caso alto & 17 Mbps & 225 bytes & 9444 \\
Red IoT local básica -- peor caso & 17 Mbps & 75 bytes & 28333 \\
Red universitaria & 17.8M paquetes / 10 min & -- & 29667 \\
PYME pequeña -- 8 Mbps & 8 Mbps & 225 bytes & 4444 \\
PYME pequeña -- 32 Mbps & 32 Mbps & 225 bytes & 17778 \\
PYME 50 empleados -- 189 Mbps & 189 Mbps & 225 bytes & 105000 \\
PYME 50 empleados -- peor caso & 189 Mbps & 75 bytes & 315000 \\
\bottomrule
\end{tabular}%
}
\end{table}

Como podemos ver en la tabla \ref{tab:cargas_red_referencia}, las cargas de red de referencia varían desde unos pocos cientos de paquetes por segundo en el caso de una red IoT local básica, hasta varios cientos de miles de paquetes por segundo en el caso de una PYME con un tráfico elevado.

Al comparar estas cargas con lo throughput obtenido por los modelos en los tres datasets, podemos observar que la mayoría de modelos presentan una capacidad de procesamiento superior a las cargas de referencia, incluso cuando se ejecutan en nuestro propio portátil.

Modelos como SVM, XGBoost, LightGBM, Random Forest y CatBoost alcanzan valores superiores al millón de muestras por segundo, lo que los posiciona como opciones viables para escenarios con cargas de red elevadas, al igual que MLP.

Por otro lado, modelos como CNN-1D ejecutados en equipo local pueden ser viables para escenarios con cargas más moderadas, pero insuficientes para casos más exigentes como 315000 paquetes por segundo, requiriendo hardware más potente.